class ARQ:
    def __init__(self, error_probability, max_retries=3):
        self.error_probability = error_probability
        self.max_retries = max_retries

    def send_packets(self, packets):
        return

    def send_packet(self, packet):
        return